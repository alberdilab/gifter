% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{get_facets}
\alias{get_facets}
\title{Get the curated facets of a GIFT or an anchor}
\usage{
get_facets(id, target = c("gift", "anchor"), db = NULL)
}
\arguments{
\item{id}{A `gift_id`, or an `anchor_id` when `target = "anchor"`.}

\item{target}{Whether `id` names a GIFT or an anchor.}

\item{db}{Optional open giftr database connection.}
}
\value{
A tibble with one row per assigned facet value.
}
\description{
Facets classify a capability; they never enter the completeness logic that
produces a call. `substrate_class` is single-valued so that it can partition
the database; `physiological_role` and the anchor facets are multi-valued,
so filtering on them returns supersets rather than partitions.
}
